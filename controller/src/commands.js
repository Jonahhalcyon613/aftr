// commands.js — controller-local re-export of the shared command registry. The
// registry mirrors the JSX COMMANDS dispatch table so bad calls are rejected
// before they ever cross the socket.
export * from '@ae-bridge/shared/commands';
